<%- include('layout', { body: `
    <h1><%= title %></h1>
    <ul>
      <% items.forEach(function(item) { %>
        <li><a href="<%= itemUrl %>/<%= item._id %>"><%= item.nome %></a></li>
      <% }) %>
    </ul>
    <a href="<%= itemUrl %>/new">Criar Novo</a>
  ` }) %>
  